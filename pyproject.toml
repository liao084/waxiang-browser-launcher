[project]
name = "waxiang-browser-launcher"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.62.0",
    "psutil>=7.2.2",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.2",
]
